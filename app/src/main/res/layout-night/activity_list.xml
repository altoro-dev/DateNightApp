<?xml version="1.0" encoding="utf-8"?>
<!--
    Dark Mode Layout for List Screen

    Darker backgrounds and adjusted colors for comfortable night viewing.
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:fitsSystemWindows="true">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#5A3D7A"
            app:title="Date Night Options"
            app:titleTextColor="#E8E0F0" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/appBarLayout"
        android:layout_above="@id/buttonContainer"
        android:padding="16dp"
        android:clipToPadding="false" />

    <TextView
        android:id="@+id/emptyText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="No options yet!\nTap + to add some"
        android:textSize="18sp"
        android:textColor="#B3B3B3"
        android:gravity="center"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:padding="16dp"
        android:paddingBottom="32dp"
        android:background="#1E1E1E"
        android:elevation="8dp">

        <Button
            android:id="@+id/pickRandomButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="🎲 Pick Random"
            android:textSize="18sp"
            android:textStyle="bold"
            android:backgroundTint="#6B4C9A"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/addButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="➕ Add Option"
            android:textSize="18sp"
            android:textStyle="bold"
            android:backgroundTint="#D968A0"
            android:textColor="#FFFFFF" />
    </LinearLayout>

</RelativeLayout>